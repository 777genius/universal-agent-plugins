api_version: v1

servers:
  docs:
    type: remote
    remote:
      url: "https://example.com/mcp"
      protocol: "streamable_http"
    targets:
{{- range .EffectiveTargets }}
      - "{{ . }}"
{{- end }}
