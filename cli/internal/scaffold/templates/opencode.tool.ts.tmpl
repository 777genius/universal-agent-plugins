import { tool } from "@opencode-ai/plugin"

export default tool({
  description: "{{.ProjectName}} helper tool.",
  args: {
    value: tool.schema.string().describe("Short value to echo back"),
  },
  async execute(args) {
    return {
      ok: true,
      value: args.value,
    }
  },
})
