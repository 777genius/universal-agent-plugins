version: 2
builds:
  - id: {{.ProjectName}}
    main: ./cmd/{{.ProjectName}}
    binary: {{.ProjectName}}
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
archives:
  - id: {{.ProjectName}}
    builds: [{{.ProjectName}}]
    format: tar.gz
