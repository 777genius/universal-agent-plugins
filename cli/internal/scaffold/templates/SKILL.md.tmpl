# {{.ProjectName}}

{{- if eq .Platform "codex" -}}
Codex skill stub. Reference your project binary after `make build`, for example `./bin/{{.ProjectName}} status`.
{{- else -}}
Agent skill stub. Reference your plugin binary after `make build`, e.g. `./bin/{{.ProjectName}}`.
{{- end -}}
