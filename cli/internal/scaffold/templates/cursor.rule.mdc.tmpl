---
description: Project-wide Cursor guidance for {{.ProjectName}}
globs:
alwaysApply: true
---

- Treat `plugin/plugin.yaml`, `plugin/mcp/servers.yaml`, and `plugin/targets/cursor-workspace/**` as the authored source of truth.
- Regenerate `.cursor/` artifacts with `plugin-kit-ai generate .` instead of editing them manually.
