api_version: v1

servers:
  service:
    description: "Replace this hosted MCP example with your real online service"
    type: remote
    remote:
      url: "https://example.com/mcp"
      protocol: "streamable_http"
    targets:
{{- range .EffectiveTargets }}
      - "{{ . }}"
{{- end }}
