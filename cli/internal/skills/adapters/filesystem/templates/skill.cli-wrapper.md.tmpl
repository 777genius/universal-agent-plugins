---
name: {{ yamlString .SkillName }}
description: {{ yamlString .Description }}
execution_mode: command
supported_agents:
  - claude
  - codex
allowed_tools:
  - shell
command: {{ yamlString .Command }}
runtime: generic
compatibility:
  notes:
    - Replace the default command with your existing CLI entrypoint.
inputs:
  - arguments from the invoking agent
outputs:
  - command-specific output
safe_to_retry: true
writes_files: false
produces_json: false
---

# {{.SkillName}}

## What it does

Describe the capability provided by the wrapped command or script.

## When to use

Explain when the agent should choose this skill.

## How to run

Document the exact non-interactive command invocation and required flags.

## Constraints

- Command must be safe for non-interactive execution.
- Declare required runtimes and CLIs in compatibility notes.
