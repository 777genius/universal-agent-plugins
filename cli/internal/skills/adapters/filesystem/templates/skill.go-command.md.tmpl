---
name: {{ yamlString .SkillName }}
description: {{ yamlString .Description }}
execution_mode: command
supported_agents:
  - claude
  - codex
allowed_tools:
  - shell
command: go run ./cmd/{{.SkillName}}
runtime: go
compatibility:
  requires:
    - go >=1.22
  repo_required: true
inputs:
  - arguments from the invoking agent
outputs:
  - stdout or stderr according to the command contract
safe_to_retry: true
writes_files: false
produces_json: false
---

# {{.SkillName}}

## What it does

Describe the capability this skill provides.

## When to use

Explain when the agent should select this skill instead of a more general command.

## How to run

Build or run the Go entrypoint and pass non-interactive arguments only.

## Constraints

- Keep output concise.
- Write diagnostics to stderr.
- Avoid interactive prompts.
